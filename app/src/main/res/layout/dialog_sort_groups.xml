<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    android:background="@drawable/button_background2"
    android:orientation="vertical"
    android:paddingBottom="12dp"
    android:paddingLeft="8dp"
    android:paddingRight="8dp"
    android:paddingTop="8dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="32dp"

        android:layout_gravity="center_vertical"
        android:layout_marginTop="4dp"
        android:alpha="0.87"
        android:background="@color/transparent"
        android:gravity="center_vertical"
        android:paddingLeft="14dp"
        android:text="Sort Groups by:"
        android:textColor="#000000"
        android:textSize="16dp"
        android:textStyle="bold" />


    <RadioGroup
        android:id="@+id/radioGroup1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="6dp"
        android:paddingTop="4dp">

        <RadioButton
            android:id="@+id/radioalpha"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:checked="true"
            android:text="  Alphabetically"></RadioButton>


        <RadioButton
            android:id="@+id/radioposts"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:checked="true"
            android:text="  Decreasing order of Posts"></RadioButton>

        <RadioButton
            android:id="@+id/radiomembers"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:text="  Decreasing order of Members"></RadioButton>

    </RadioGroup>
</LinearLayout>